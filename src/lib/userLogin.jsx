import { createSlice } from "@reduxjs/toolkit";
import { useState } from "react";


export let data = createSlice({
    name:"",
    initialState : [userLogin, setuserLogin] = useState(null),
    reducers:"",

})

export let dataReducer = data.reducer